# local run artefacts
.DS_Store
kubeconfig

# the output*.log files are the evidence the READMEs quote, so they are tracked
# on purpose. any other stray log is not.
*.log
!*/output.log
!*/output-*.log
