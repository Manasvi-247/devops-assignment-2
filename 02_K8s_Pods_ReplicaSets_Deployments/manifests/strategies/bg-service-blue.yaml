# Live traffic points at the blue slot.
apiVersion: v1
kind: Service
metadata:
  name: myapp-service
  namespace: strategies
spec:
  type: ClusterIP
  selector:
    app: myapp
    slot: blue
  ports: [{name: http, port: 80, targetPort: 5678}]
