# Selector matches BOTH tracks, so the pod ratio is the traffic split.
apiVersion: v1
kind: Service
metadata:
  name: myapp-canary-service
  namespace: strategies
spec:
  type: ClusterIP
  selector:
    app: myapp-canary
  ports: [{name: http, port: 80, targetPort: 5678}]
